# Minimal Jupyter Notebook Project

This workspace contains a single Jupyter notebook named `notebook.ipynb`.

How to open:
- Open this folder in VS Code.
- Install the Python and Jupyter extensions if not already installed.
- Click on `notebook.ipynb` to open the notebook editor.

This is a minimal scaffold created by the assistant.